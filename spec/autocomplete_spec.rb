# frozen_string_literal: true

RSpec.describe AutoComplete do
  it 'Marshalls from JSON' do
    expect(false).be false
  end

  it 'Marshalls to JSON' do
    expect(false).be false
  end
end
